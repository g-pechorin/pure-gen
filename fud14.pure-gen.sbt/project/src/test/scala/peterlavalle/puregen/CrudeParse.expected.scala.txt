import org.graalvm.polyglot.{HostAccess, Value}
import peterlavalle.puregen.{TEnum, TModule}
import scala.reflect.ClassTag
import peterlavalle._

package foo.bar {
	trait CrudeParse extends TModule {
		def openFlap(a0: String): TModule.Event[String]
		def openShip(a0: Int): TModule.Sample[Float]
		def openVampire(): TModule.Signal[Double]
	}
}
